import { FormatRemainingTimePipe } from './format-remaining-time.pipe';

describe('FormatRemainingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRemainingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
